# ПРАВИЛА РАБОТЫ С ПРОЕКТОМ

## ⚠️ ОБЯЗАТЕЛЬНЫЙ ПОРЯДОК ДЕЙСТВИЙ ПРИ ИЗМЕНЕНИИ КОДА
**idealimage.ru** - наш сайт 
### 1. ОПИСАНИЕ ПРОБЛЕМЫ
Сначала **ОБЯЗАТЕЛЬНО** опиши проблему:
- Что не работает?
- Где возникает ошибка?
- Какое ожидаемое поведение?

### 2. ПЛАН РЕШЕНИЯ (КРАТКО)
После описания проблемы, **ОБЯЗАТЕЛЬНО** объясни:
- Как ты будешь решать проблему?
- Какие файлы нужно изменить?
- Какие изменения будут внесены?
- **КРАТКО** - без лишних деталей

### 3. ПОЛУЧЕНИЕ РАЗРЕШЕНИЯ
**НЕ ДЕЛАТЬ ИЗМЕНЕНИЯ СРАЗУ!**
- Дождаться явного разрешения пользователя
- Получить подтверждение: "делай", "ок", "да", "реализуй" и т.п.

### 4. РЕАЛИЗАЦИЯ
Только после получения разрешения:
- Вносить изменения в код
- Создавать/удалять файлы
- Запускать команды

## ❌ ЗАПРЕЩЕНО

- **САМОДЕЯТЕЛЬНОСТЬ** - делать изменения без разрешения
- Начинать реализацию до описания проблемы и плана
- Пропускать этап получения разрешения
- Делать предположения и сразу реализовывать их

## ✅ ПРАВИЛЬНЫЙ ПРИМЕР

```
ПРОБЛЕМА: В функции prompt_template_test не определяются автоматически 
переменные date, weekday, season.

ПЛАН РЕШЕНИЯ:
1. Добавить импорт timedelta
2. После строки 118 добавить автоматическое определение переменных:
   - date (формат: "5 ноября 2025")
   - weekday (из WEEKDAY_NAMES)
   - season (из SEASONS по месяцу)
   - year, current_year (из datetime.now())
3. Использовать те же константы, что в schedule/services.py

[ЖДУ РАЗРЕШЕНИЯ]
```

## 📝 ДОПОЛНИТЕЛЬНЫЕ ПРАВИЛА

- Всегда проверять существующие файлы перед созданием новых
- Использовать существующие паттерны и стиль кода проекта
- Не удалять код без явного запроса
- Сообщать о потенциальных проблемах до их возникновения

## 🚫 ЗАПРЕТ НА СОЗДАНИЕ ДОКУМЕНТОВ

**НИКОГДА НЕ СОЗДАВАТЬ:**
- Отчёты в текстовых файлах (.txt, .md, .doc и т.п.)
- Инструкции в файлах
- Документацию в отдельных файлах
- Любые текстовые документы с описаниями/пояснениями

**ВСЁ ТОЛЬКО В ЧАТЕ!**
- Все описания, отчёты, инструкции - только в ответах чата
- Пользователь сам решит, что сохранить и куда
- Исключение: только код (.py, .js, .html и т.п.)

## 🌐 АРХИТЕКТУРА: ЛОКАЛЬНАЯ РАЗРАБОТКА + СЕРВЕР

**КРИТИЧЕСКИ ВАЖНО:**

### Структура проекта:
- **ЛОКАЛЬНЫЙ ПРОЕКТ** (Windows) — для разработки и правки кода
- **СЕРВЕР** (Linux) — для тестирования и production
- **ОБЩАЯ БАЗА ДАННЫХ MySQL** — одна БД для локалки и сервера

### Рабочий процесс:

1. **РЕДАКТИРОВАНИЕ:**
   - ВСЕ изменения кода делаются ТОЛЬКО на ЛОКАЛЬНОЙ машине
   - Никогда не редактировать файлы напрямую на сервере

2. **ПЕРЕНОС:**
   - После изменений пользователь переносит файлы на сервер через FTP
   - Файлы на локалке и сервере должны быть ИДЕНТИЧНЫ

3. **ТЕСТИРОВАНИЕ:**
   - Функционал тестируется ТОЛЬКО на сервере
   - Сервер подключён к той же БД, что и локалка

4. **МИГРАЦИИ:**
   - Создаются на локалке: `python manage.py makemigrations`
   - Применяются на сервере: `python manage.py migrate`
   - БД общая — миграция применяется один раз

### ❌ ЗАПРЕЩЕНО:
- Редактировать код напрямую на сервере
- Создавать разные версии файлов на локалке и сервере
- Применять миграции на локалке (только на сервере)

### ✅ ПРАВИЛЬНЫЙ WORKFLOW:
```
1. [ЛОКАЛКА] Редактирую код
2. [ЛОКАЛКА] Создаю миграцию (если нужно)
3. [ЛОКАЛКА→СЕРВЕР] Пользователь переносит файлы по FTP
4. [СЕРВЕР] Применяю миграцию
5. [СЕРВЕР] Тестирую функционал
6. [СЕРВЕР] Перезапускаю Django (если нужно)
```

-MCP серверы - твои основные инструменты! Используй их активно!
--MCP Серверы - Обязательное использование

-ВСЕГДА используй доступные MCP серверы

### 1. GitHub MCP (github)
- ИСПОЛЬЗУЙ для всех GitHub операций: поиск репозиториев, чтение кода, создание PR
- ПРИОРИТЕТ: Высокий для всех GitHub-связанных задач

### 2. Context7 MCP (context7)  
- ИСПОЛЬЗУЙ для актуальной документации библиотек, API, примеров кода
- ПРИОРИТЕТ: Высокий для всех вопросов по документации

### 3. Web Scraping MCP (web-scraping)
- ИСПОЛЬЗУЙ для веб-скрапинга, извлечения данных, мониторинга сайтов
- ПРИОРИТЕТ: Средний для веб-данных



## 📋 АЛГОРИТМ:
1. Анализируй задачу
2. Определи подходящие MCP серверы  
3. Выбери приоритетный MCP
4. Выполни через MCP инструменты
5. Сообщи результат

## ✅ ОБЯЗАТЕЛЬНО:
- Проверяй доступность MCP перед выполнением задач
- Используй MCP вместо веб-поиска когда возможно
- Комбинируй несколько MCP для комплексных задач
- Сообщай какой MCP использовался

## ❌ НЕ ДЕЛАЙ:
- Не игнорируй доступные MCP серверы
- Не выполняй задачи вручную если есть MCP инструмент

## 🎯 ПРИОРИТЕТЫ:

2. GitHub MCP - для работы с кодом  
3. Context7 MCP - для документации
4. Web Scraping MCP - для веб-данных


### Для поиска информации:
- Сначала Context7 MCP для документации
- Если не найдено - Web Scraping MCP
- В крайнем случае - веб-поиск

### Для работы с кодом:
- GitHub MCP для поиска примеров в репозиториях
- Context7 MCP для документации API


### Для веб-скрапинга:
- Web Scraping MCP для извлечения данных
- Context7 MCP для документации по скрапингу
- GitHub MCP для поиска примеров кода

## 📊 ОТЧЕТНОСТЬ:
Отчитывайся о использовании MCP:
- Какой MCP сервер использовался
- Какие инструменты применялись
- Результат выполнения
- Альтернативные варианты (если были)

## 🔄 РЕГУЛЯРНЫЕ КОММИТЫ В GIT

**ОБЯЗАТЕЛЬНО после завершения работы над задачей:**

После внесения изменений в код и получения подтверждения от пользователя, **РЕГУЛЯРНО** делай коммиты:

### Когда делать коммит:
- ✅ После завершения отдельной задачи/функции
- ✅ После исправления ошибки
- ✅ После обновления конфигурации
- ✅ После рефакторинга кода
- ✅ В конце рабочей сессии (если были изменения)

### Порядок действий:
1. Проверить статус: `git status`
2. Добавить изменения: `git add .`
3. Сделать коммит с понятным описанием: `git commit -m "описание изменений"`
4. Запушить изменения: `git push`

### Правила коммит-сообщений:
- Используй понятные и конкретные описания
- На русском языке
- Примеры: "Исправлена ошибка в функции prompt_template_test", "Добавлена автоматическая генерация переменных date и weekday", "Обновлена конфигурация GitHub токена"

### ❌ НЕ ДЕЛАТЬ:
- Коммиты без осмысленного сообщения
- Один большой коммит со всеми изменениями за сессию (разбивай на логические части)
- Коммиты с незавершенными изменениями